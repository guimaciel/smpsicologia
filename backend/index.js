const express = require("express");
const app = express();
const port = 3000;

// const {getTopicos} = require("./routes/topicos");
// const {getPaciente} = require("./routes/pacientes");
// const { getProfissional } = require('./routes/profissional');

// app.get('/', (req, res) => {
//    res.send('Hello World!');
// });

// app.get("/topicos", getTopicos)

// app.get('/paciente/:id', getPaciente);

// app.get('/profissional/:id', getProfissional);

// app.listen(port, () => {
//    console.log(`Listening at http://localhost:${port}`);
// });
